'use client';

import * as React from 'react';
import '@/lib/env';
import { HeroSection } from '@/components/hero';

export default function HomePage() {
  return (
    <main>
      <HeroSection />
    </main>
  );
}
